{
  "name": "fomod-installer",
  "version": "0.0.12",
  "description": "Installer for xml and c# fomods",
  "main": "dummy.js",
  "scripts": {
    "buildDev": "node build.js Debug && copyfiles -f Build/bin/Debug/*.dll dist/ && copyfiles -f Build/bin/Debug/*.pdb dist/",
    "build": "node build.js && copyfiles -f Build/bin/Release/*.dll dist/",
    "install": "npm run build"
  },
  "author": "Black Tree Gaming Ltd.",
  "license": "GPL-3.0",
  "devDependencies": {},
  "dependencies": {
    "copyfiles": "^1.0.0",
    "msbuild": "^1.0.1"
  }
}
